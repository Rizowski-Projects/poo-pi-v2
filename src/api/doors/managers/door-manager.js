import database from '../../../db';
import createLogger from '../../../logger';


export default {
  createDoor: (door) => {
    database()
      .then(() =>{
        
      });
  },
  updateStatus: (id, status) => {

  },
  deleteDoor: (id) =>{

  }
}
